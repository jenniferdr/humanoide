cmake_minimum_required(VERSION 2.8)
project( detectar )
find_package( OpenCV REQUIRED )
include_directories(/home/juli/OpenCvTesis)
add_executable( detectar detectar.cpp )
target_link_libraries( detectar	 ${OpenCV_LIBS} )
